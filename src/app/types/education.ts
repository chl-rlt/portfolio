export type Education = {
  logo: Logo;
  title: string;
  description: string;
  role: string;
  date: string;
};

type Logo = {
  key: string;
  device: string;
  h: string;
  w: string;
  url: string;
};
